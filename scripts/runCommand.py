############################################################################################################################################################
# Running commands in parallel
############################################################################################################################################################


import os
import subprocess


def runCommand(eachinput):
    Run,cmd=eachinput
    if "<" not in cmd:
        os.system(cmd)
    else:
        subprocess.check_call(['bash', '-c', cmd])
